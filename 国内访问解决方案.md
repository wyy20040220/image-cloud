# 国内访问解决方案

Vercel 在国内某些地区可能访问超时，以下是几种解决方案：

## 方案 1：绑定自定义域名（推荐）

在 Vercel 绑定一个自己的域名，国内访问会快很多。

1. 在 Vercel 项目 → Domains → 添加域名
2. 在域名服务商添加 CNAME 记录指向 Vercel
3. 等待 DNS 生效（几分钟到几小时）

## 方案 2：Cloudflare CDN 加速

1. 注册 Cloudflare 账号
2. 添加你的域名到 Cloudflare
3. 修改 DNS 为 Cloudflare 提供的
4. 开启 CDN 加速

## 方案 3：部署到国内可访问平台

### Railway（免费，国内可访问）
```bash
# 安装 Railway CLI
npm i -g @railway/cli

# 登录
railway login

# 初始化项目
railway init

# 部署
railway up
```

### Render（免费，国内可访问）
1. 访问 https://render.com
2. 用 GitHub 登录
3. New Web Service → 选择你的 GitHub 仓库
4. 配置：
   - Build Command: `npm install`
   - Start Command: `npm start`
5. 部署

## 方案 4：使用 Gitee Pages

如果只需要静态页面展示：
1. 在 Gitee 创建仓库
2. 开启 Gitee Pages 服务
3. 国内访问速度最快

## 方案 5：腾讯云/阿里云服务器

学生套餐 10元/月，送域名，最稳定。

---

## 快速测试方法

先用命令行测试能否访问：
```bash
curl -I https://image-cloud.vercel.app
```

如果超时，说明需要换方案。

## 推荐

如果只是个人使用，**方案 1 绑定自定义域名** 最简单。
如果需要长期稳定，**方案 3 Railway** 是最好的免费选择。
