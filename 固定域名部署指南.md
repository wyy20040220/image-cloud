# 🚀 Railway 固定域名部署指南

## 优点

- ✅ **永久免费**（每月有额度，个人使用足够）
- ✅ **固定域名**（如 `https://image-cloud.up.railway.app`）
- ✅ **自动 HTTPS**（小程序必需）
- ✅ **无需维护**（自动部署，持续运行）

---

## 部署步骤

### 第一步：准备 GitHub 仓库

1. 访问 https://github.com 创建账号（如有请跳过）
2. 创建新仓库，名称如 `image-cloud`
3. 将本地代码推送到仓库：

```bash
# 进入项目目录
cd /Users/wyy/Desktop/图云

# 初始化 git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 连接远程仓库（将 xxx 换成你的用户名）
git remote add origin https://github.com/xxx/image-cloud.git

# 推送
git push -u origin main
```

---

### 第二步：部署到 Railway

1. 访问 https://railway.app
2. 点击 "Start a New Project"
3. 选择 "Deploy from GitHub repo"
4. 授权 Railway 访问你的 GitHub
5. 选择 `image-cloud` 仓库
6. 点击 "Deploy Now"

---

### 第三步：获取固定域名

部署完成后：

1. 在项目页面点击 "Settings"
2. 找到 "Environment" → "Public Domain"
3. 点击 "Generate Domain"
4. 获得固定域名如：`https://image-cloud-production-xxx.up.railway.app`

---

### 第四步：配置小程序

修改 `mini-program/app.js`：

```javascript
App({
  globalData: {
    // Railway 固定域名
    apiBaseUrl: 'https://image-cloud-production-xxx.up.railway.app'
  },
  ...
});
```

---

### 第五步：小程序后台配置

登录 [微信公众平台](https://mp.weixin.qq.com)：

1. 开发 → 开发设置 → 服务器域名
2. 添加：
   - `request` 合法域名：`https://image-cloud-production-xxx.up.railway.app`
   - `downloadFile` 合法域名：`https://image-cloud-production-xxx.up.railway.app`

---

## 自动部署

以后每次推送代码到 GitHub，Railway 会自动重新部署！

```bash
git add .
git commit -m "更新功能"
git push
```

---

## 替代方案：Render

如果 Railway 额度用完，可以使用 Render（也是免费的）：

1. 访问 https://render.com
2. 创建 Web Service
3. 连接 GitHub 仓库
4. 配置：
   - Build Command: `npm install`
   - Start Command: `node server.js`
5. 获得固定域名如 `https://image-cloud.onrender.com`

---

## 常见问题

### 1. Railway 免费额度是多少？
- 每月 $5 免费额度
- 个人项目足够使用
- 超出后会暂停，下个月恢复

### 2. 图片保存在哪里？
- 上传到 Railway 的文件系统是临时的
- **重要**：重启后会丢失！
- 如需永久保存，需要连接云存储（如 AWS S3、Cloudinary）

### 3. 如何备份图片？
建议定期将图片下载到本地备份，或使用外部存储服务。

---

## 快速脚本

创建 `deploy.sh`：

```bash
#!/bin/bash
echo "🚀 部署到 Railway..."
git add .
git commit -m "update"
git push
echo "✅ 已推送，Railway 会自动部署"
```

---

## 总结

| 平台 | 固定域名 | 免费额度 | 推荐度 |
|------|----------|----------|--------|
| Railway | ✅ | $5/月 | ⭐⭐⭐⭐⭐ |
| Render | ✅ | 750小时/月 | ⭐⭐⭐⭐ |
| Vercel | ✅ | 无限 | ⭐⭐⭐ (需要改动) |

**推荐 Railway**，部署最简单！
