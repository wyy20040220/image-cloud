# 公网访问配置指南

## 方案一：ngrok 内网穿透（推荐，免费快速）

### 步骤

1. **确保服务器在运行**
   ```bash
   npm start
   ```

2. **新开终端运行 ngrok**
   ```bash
   npx ngrok http 3000
   ```

3. **获取公网地址**
   
   运行后会显示类似：
   ```
   Forwarding  https://abc123.ngrok-free.app -> http://localhost:3000
   ```

4. **使用公网地址**
   
   网页访问：`https://abc123.ngrok-free.app`
   
   图片URL：`https://abc123.ngrok-free.app/uploads/xxx.jpg`

### 小程序配置

修改 `mini-program/app.js`：
```javascript
apiBaseUrl: 'https://abc123.ngrok-free.app'  // 你的 ngrok 地址
```

---

## 方案二：部署到免费云平台

### Railway（推荐）

1. 访问 https://railway.app
2. 用 GitHub 账号登录
3. 新建项目 → 从 GitHub 导入本项目
4. 自动部署完成

### Render

1. 访问 https://render.com
2. 注册账号
3. New Web Service → 连接 GitHub 仓库
4. 选择免费套餐部署

---

## 方案三：使用其他内网穿透工具

| 工具 | 命令 | 特点 |
|------|------|------|
| ngrok | `npx ngrok http 3000` | 最流行，免费版有随机域名 |
| cloudflared | `cloudflared tunnel --url http://localhost:3000` | Cloudflare出品，稳定 |
| localtunnel | `npx localtunnel --port 3000` | 简单，免费子域名 |

---

## 注意事项

1. **ngrok 免费版限制**
   - 每次启动域名会变（可以付费固定）
   - 每月有流量限制（一般够用）

2. **小程序要求**
   - 真机调试需要 https
   - 需要在小程序后台配置服务器域名

3. **生产环境建议**
   - 使用 Railway 或 Render 永久部署
   - 配置自定义域名
